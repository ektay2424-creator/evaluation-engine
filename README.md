# evaluation-engine
Here's a solid one-liner for the GitHub repo description field: "Automated regression detection and deployment-readiness evaluation for AI agents — compares agent behavior across versions using deterministic metrics, semantic tool-call similarity, and rule-based classification to decide if a new version is safe to ship.
